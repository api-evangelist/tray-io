naftiko: 1.0.0-alpha2
info:
  label: Tray.io Platform API — Users
  description: 'Tray.io Platform API — Users. 2 operations. Lead operation: List Users. Self-contained Naftiko capability
    covering one Tray Io business surface.'
  tags:
  - Tray Io
  - Users
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    TRAY_IO_API_KEY: TRAY_IO_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-users
    baseUri: https://api.tray.io/core/v1
    description: Tray.io Platform API — Users business capability. Self-contained, no shared references.
    resources:
    - name: users
      path: /users
      operations:
      - name: listusers
        method: GET
        description: List Users
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: users-invite
      path: /users/invite
      operations:
      - name: inviteuser
        method: POST
        description: Invite User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.TRAY_IO_API_KEY}}'
  exposes:
  - type: rest
    namespace: platform-users-rest
    port: 8080
    description: REST adapter for Tray.io Platform API — Users. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users
      name: users
      description: REST surface for users.
      operations:
      - method: GET
        name: listusers
        description: List Users
        call: platform-users.listusers
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users/invite
      name: users-invite
      description: REST surface for users-invite.
      operations:
      - method: POST
        name: inviteuser
        description: Invite User
        call: platform-users.inviteuser
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-users-mcp
    port: 9090
    transport: http
    description: MCP adapter for Tray.io Platform API — Users. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: list-users
      description: List Users
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: platform-users.listusers
      outputParameters:
      - type: object
        mapping: $.
    - name: invite-user
      description: Invite User
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: platform-users.inviteuser
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
